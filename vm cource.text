2.3 4th 不能以数字开头；sizeof 为保留字；x*y 含非法字符*
    temp-2 含非法字符-； isn't 含非法字符' enum 为保留字
2.4 合法，long型；非法，字符型常量使用’需要转义；合法，double型；
    非法，16进制中不包含l，g；合法，int型；合法。字符串型；合法，long double型；
    非法，不包含整数部分/小数部分；合法，8进制形式的unsigned long型；
    合法，字符型；合法，字符型；合法，16进制unsigned；合法，字符型；合法，double型；
    合法，字符型；合法，float；非法，字符串中”需要使用转义序列；合法，字符串常量；合法，字符常量；
2.6 由于第一个分号结束了int的定义，故而b的类型不明确；h的变量未赋初值（可以接受的错误）；对int型变量赋予double
    对常量未赋值；对变量赋值不明确;不可使用此形式为变量赋值
2.7  4   0   1   6    3    